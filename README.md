# Langauge compiler thing idk we still need a name

## How to run
With docker
```
sudo docker build -t tobe .
sudo docker run tobe
```

Otherwise make sure you have `make`, `llvm-devel` (or whatever llvm thingy is on ur distro), and `g++` installed then
```
make 
./tobe
```
